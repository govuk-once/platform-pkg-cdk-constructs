### Log in to AWS
# Build Plan
# 
name: Update Repository (Prod)
on:
  push:
    tags: 'v*'

permissions:
  id-token: write
  contents: read

jobs:
  terraform-plan:
    name: Terraform Plan
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Configure Aws Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.DEPLOYMENT_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION }}
          role-session-name: GH-Actions-Deploy-Prod-Constructs

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v1
        with:
          terraform_version: 1.14.5

      - name: Initialize Terraform
        run: terraform init --backend-config=prod.config
        
      - name: Run Terraform Plan
        run: terraform plan -var-file=prod.tfvars -out=tfplan.bin

      - name: Show Terraform Plan
        run: terraform show --no-color tfplan.bin >> $GITHUB_STEP_SUMMARY

      - name: Store JSON Terraform Plan
        uses: actions/upload-artifact@v4
        with:
          name: terraform-plan
          path: tfplan.bin
          retention-days: 1

  terraform-apply:
    name: Terraform Apply
    environment: prod
    runs-on: ubuntu-latest
    needs: [terraform-plan]
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Configure Aws Credentials
        uses: aws-actions/configure-aws-credentials@56d6a583f00f6bad6d19d91d53a7bc3b8143d0e9 # v5.1.1
        with:
          role-to-assume: ${{ secrets.DEPLOYMENT_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v1
        with:
          terraform_version: 1.14.5

      - name: Initialize Terraform
        run: terraform init --backend-config=prod.config

      - name: Download Terraform Plan Artifact
        uses: actions/download-artifact@v2
        with:
          name: terraform-plan
          path: .

      - name: Run Terraform Apply
        run: terraform apply tfplan.bin
